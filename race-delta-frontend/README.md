# RaceDelta — Frontend (Dark UI) - Stats charts updated

Run locally:
1. npm install
2. npm run dev

The Stats page now renders charts inside white background cards while keeping other components dark.
